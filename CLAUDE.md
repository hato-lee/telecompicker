# 통신비피커 — 작업 규칙

이 파일은 이 폴더에서 대화를 시작할 때마다 자동으로 읽힙니다.
원본은 베네핏 우산 공통 규칙(`/Users/hato/Projects/benefit/CLAUDE.md`)이고, 이 집에 맞게 승계한 것이다.
모델 배정은 `docs/MODEL-ROLES.md`가 원본이다.

## 0. 이 집이 뭐 하는 곳인가

베네핏 우산의 3번째 칸, **통신비피커** — 요금제·알뜰폰 비교 (데이터형, 독립 저장소).
설계: `docs/superpowers/specs/2026-09-01-telecompicker-design.md`
결정 대장: `docs/data-collection/DECIDED.md` — **갈래는 이 문서를 맨 먼저 읽는다.**
⭐ 결합할인이 나중의 본체다(하토님 2026-09-01). 이번 범위는 요금제·알뜰폰만.

## 1. 긴 작업이 중간에 끊기지 않게

**왜 생겼나:** 2026-08-28 새벽(카드피커), 「이제 4차 갈래를 띄우겠습니다」라고
**글로 예고만 하고 차례를 끝냈다.** 깨울 것이 없어서 5시간 47분 동안 아무 일도 일어나지 않았다.

### 규칙 1 — 예고하고 끝내지 않는다
「이제 ~하겠습니다」를 쓴 뒤 차례를 끝내는 것은 **금지.** 순서를 뒤집는다: **먼저 띄우고, 그다음에 보고한다.**

### 규칙 2 — 「깨울 것」 없이 차례를 끝내지 않는다
일이 남아 있으면, 끝내기 전에 확인한다: 도는 갈래가 있는가? 시계(CronCreate)가 걸려 있는가?
**둘 다 없으면 차례를 끝내면 안 된다.** 하나를 만들고 끝낸다.

### 규칙 3 — 하토님이 자리를 비우시면 제일 먼저 시계를 건다
「자고 일어날 동안 해놔」를 받으면 **일보다 먼저** 시계를 건다:

    CronCreate({ cron: "17,47 * * * *", recurring: true,
      prompt: "docs/NIGHT-LEDGER.md를 읽어라. 체크 안 된 줄이 있으면 이어서 하고,
               다 끝났으면 아무것도 하지 말고 조용히 끝내라." })

분은 0분·30분을 피한다. 시계는 이 대화가 살아 있는 동안만 있다.

### 규칙 4 — 장부를 먼저 쓰고 일한다
여러 라운드짜리 일은 시작 전에 `docs/NIGHT-LEDGER.md`에 할 라운드를 다 적고, 끝날 때마다 체크한다.

### 규칙 5 — 판은 라운드마다 갱신한다
`docs/OPEN-ISSUES.md`(할 일 판) 갱신을 마지막으로 미루지 않는다. 갈래가 도는 동안 판을 쓴다.

### 규칙 6 — 검사는 **배포가 돌리는 그 명령으로** 한다
- 배포에 영향을 주는 확인은 **`npm run build`**. 부분만 볼 때도 **`npx tsc -b`**(`--noEmit` 아님).
- ⚠️ `npx vitest run`이 통과해도 빌드는 깨질 수 있다 — vitest는 타입을 안 본다.
- 갈래 지시문에도 `npx tsc -b`를 적는다.
- 「검사 N개 통과」만으로 「배포된다」고 말하지 않는다.

## 2. 하토님과 일하는 법

- 하토님은 비개발자시다. **쉬운 한국어로, 기술 용어는 풀어서** 쓴다.
- 화면 이야기는 **실제로 렌더해서 눈으로 본 뒤**에 말한다. curl만으로 레이아웃은 못 본다.
- 「0건」이 나오면 **일부러 깨진 입력을 넣어** 검사가 실패하는지 먼저 본다.
- 영향도는 실제 화면이 쓰는 경로로 잰다. 대리 지표는 자릿수 단위로 틀린다.
- **부풀리지 않는다.** 못 한 것은 못 했다고 적는다.
- 하토님 게이트 3개: ✅설계(2026-09-01) → ⬜화면 첫 모습 → ⬜개업(배포).
  결정이 나오면 **그날 안에** `docs/data-collection/DECIDED.md`에 적는다.

## 3. 갈래(병렬 작업) 규약

- `git worktree add -b lane-<이름> <경로> HEAD` 뒤
  `ln -s /Users/hato/Projects/telecompicker/node_modules <경로>/node_modules`
- 정리할 때 심볼릭 링크를 **먼저** `rm -f` 한다.
- ⚠️ **`node_modules` 링크가 갈래 커밋에 섞이지 않았는지 합치기 전에 확인한다.**
  `git ls-files -s node_modules`가 뭔가 뱉으면 합치기 전에 `git rm --cached node_modules`.
  `.gitignore`에는 슬래시 없는 `node_modules` 줄도 함께 둔다(링크까지 막으려고).
- `npm ci`는 목록에 없는 것을 지운다. playwright는 목록에 안 넣는다(화면 검증용).
  지워졌으면 `npm i playwright --no-save`.
- **파일 소유권을 갈래마다 갈라 준다.** 같은 파일은 한 라운드에 한 갈래만.
- ⭐ **갈래 지시문에 「이미 결정된 것」(`docs/data-collection/DECIDED.md`)을 반드시 붙인다.**
  갈래는 「일부러 안 담은 줄」과 「빠뜨린 줄」을 구분할 방법이 없다.
- 공용 문서(`docs/OPEN-ISSUES.md`·`DECIDED.md`·수집 규약 문서)는 **갈래가 건드리지 않는다.** 총괄만 쓴다.
- ⭐ **길을 찾으면 그 자리에서 계열 `README.md`를 고친다.**
  스냅샷은 「무엇을 봤나」, README는 「어떻게 보나」 — 둘은 다른 물건이다.
  못 찾았을 때도 **뒤진 곳 목록**을 적는다.
- ⚠️ **갈래에게 「검사를 돌려라」고 할 때 그 검사가 진짜인지 먼저 확인한다.**
  실데이터를 안 보는 합성 예제 시험은 항등식이다.
- **갈래마다 모델을 갈라 붙인다** — `docs/MODEL-ROLES.md`. `model`을 반드시 명시한다.
- ⚠️ **`git add -A`로 합치지 않는다.** 내 갈래의 파일 경로를 하나씩 `git add` 하고,
  커밋 전에 `git status`를 눈으로 읽는다.

## 4. 수집 규약 (카드피커 승계)

- 원문 스냅샷은 `data/carriers/<계열>/sources/<요금제id>-<날짜>-<목적>.md` — **덮어쓰지 않는다.**
- 모든 수정은 근거(출처)와 함께 `memo`에 한 줄 이력. **「안 담았다」도 이력이다.**
- 확인 못 한 수치는 지어내지 않는다 — `memo`에 「원문이 말하지 않는다」.
- 값마다 원문 링크 + 확인 날짜(`checkedAt`)를 갖는다. 이게 화면 배지가 된다.
