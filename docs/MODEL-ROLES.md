# 통신비피커 — 갈래에 어느 모델을 붙이나

원본: `/Users/hato/Projects/benefit/docs/MODEL-ROLES.md` (2026-08-31) → 데이터형 칸 표를 승계.
**한 줄로: 일의 무게에 모델을 맞춘다.** 갈래를 띄울 때 `Agent` 도구의 `model`을 **반드시 명시한다.**
안 적으면 총괄과 같은 모델로 붙어서 이 규칙이 조용히 무효가 된다.

| 일의 종류 | 모델 |
|---|---|
| 원문 찾기·긁기 (스크립트는 총괄이 완성해서) | `haiku`·`sonnet` |
| 기계적 반복 | `haiku` |
| 단순 수집·대조 | `sonnet` |
| 판정이 섞인 수집, 처음 뚫는 계열 | `opus` (Fable 맞대결 결과 2026-08-30: 판정 수집은 opus 유지) |
| 코드·화면·검사 | `opus` |
| 설계·대안 견주기 | `fable` |

**Sonnet 5 시험 운행(하토님 승인 2026-08-28):** 첫 수집 라운드에서 갈래 한둘을
Sonnet 5로 돌리고, 총괄이 원문 대조한 뒤 확대 여부를 정한다.

**시험 결과 (2026-09-01, kt엠모바일 갈래 — 길 뚫린 계열 104개 수집): 합격.**
- 총괄 원문 대조 4건 전수 일치 (값·데이터·속도제어·통화 무제한). 검사 3종 자체 수행.
- 단말결합 함정 카테고리(이름에 안 드러남)를 원문 문구로 스스로 걸러냄. 판정 유보 지시도 정확히 지킴
  (기간 문구 없음 → 전부 정가+memo, 그리고 「전 사업자가 이 패턴이면?」을 보고서에 올려 총괄 판정을 끌어냄).
- **판정: 길이 뚫린(README 있는) 계열의 수집·재수집은 sonnet에게 확대해도 된다.**
  처음 뚫는 계열·판정 섞인 수집은 계속 opus.

## 공통 경고 (카드피커에서 배운 그대로)

- ⚠️ **값싼 모델로 내리기 전에 물을 것 — 「지시문 안에 답이 다 있는가?」** 없으면 지어낸다.
  `haiku`·`sonnet`에게는 판정을 시키지 않는다 — 「모르겠으면 「원문이 말하지 않는다」라고
  적어라」를 지시문에 반드시 넣는다.
- ⚠️ **싸다고 갈래를 더 띄우지 않는다.** 병목은 합치는 총괄이다.
- **품질 확인**: 값싼 모델로 돌린 라운드는 총괄이 결과 몇 개를 원문과 직접 대조한다.
  어긋나면 그 종류의 일을 한 단계 위로 올리고, 이 표를 고친다.
